"use client";

import React from "react";

/**
 * WelcomeSection removed — component intentionally returns null so the "Chào mừng tới SSSTUDY"
 * section is no longer rendered. Kept as a component to avoid breaking imports.
 */
const WelcomeSection: React.FC = () => {
  return null;
};

export default WelcomeSection;